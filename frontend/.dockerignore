# Dependencies — never send to Docker, installed fresh inside the image
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output — rebuilt inside the image
.next
out

# Env files — injected at runtime via docker-compose, not baked into image
.env
.env.local
.env.development
.env.production
.env*.local

# Dev tooling — not needed for the build
.git
.gitignore
.vscode
.idea
*.md
README*

# Test artifacts
coverage
__tests__
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx

# OS noise
.DS_Store
Thumbs.db
