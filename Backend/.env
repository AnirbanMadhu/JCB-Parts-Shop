# Database Configuration
DATABASE_URL="postgresql://jcbuser:jcbpassword@localhost:5432/jcbdb?schema=public"

# Server Configuration
PORT=4001
NODE_ENV=production

# JWT Secret for Authentication (change this in production!)
JWT_SECRET=your-secret-key-change-in-production-to-something-very-secure

# PostgreSQL Database Credentials (for docker-compose)
POSTGRES_USER=jcbuser
POSTGRES_PASSWORD=jcbpassword
POSTGRES_DB=jcbdb

# Email Configuration (Optional - for password reset emails)
# If not configured, reset tokens will be logged to console in development
FRONTEND_URL=http://localhost:3000
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=koushikm203@gmail.com
SMTP_PASS=gvzpwgraasjaqwhy
SMTP_FROM="JCB Parts Shop <koushikm203@gmail.com>"
